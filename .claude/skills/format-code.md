# format-code

Format Python code using Black code formatter.

## Instructions

1. Run Black on the api directory: `uv run black api/`
2. Show the user which files were reformatted (if any)
3. If files were changed, inform the user that the code has been formatted
4. Remind the user to review the changes before committing
