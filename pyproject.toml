[project]
name = "fastapi-vercel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "pydantic>=2.10.4",
    "uvicorn>=0.32.1",
]

[dependency-groups]
dev = [
    "black>=25.11.0",
]
